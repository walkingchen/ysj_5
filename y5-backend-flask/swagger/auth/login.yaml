---
tags:
  - Auth
parameters:
  - name: body
    in: body
    required: true
    schema:
      id: auth_login
      required:
        - username
        - password
      properties:
        username:
          type: string
          description: username
          required: true
        password:
          type: string
          description: password
          required: true
responses:
  200:
    description: return room with details
    schema:
      id: Response
      properties:
        result_code:
          type: string
        result_msg:
          type: string
        data:
          type: string