---
tags:
  - Private Message Pool && Assign
parameters:
  - name: file
    in: formData
    type: file
    required: true
    description: upload private messages pics
responses:
  200:
    description: upload private messages pics
    schema:
      id: Response
      properties:
        result_code:
          type: string
        result_msg:
          type: string
        data:
          type: string
