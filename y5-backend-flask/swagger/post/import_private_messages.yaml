---
tags:
  - Private Message Pool && Assign
parameters:
  - name: file
    in: formData
    type: file
    required: true
    description: upload private messages
responses:
  200:
    description: upload private messages
    schema:
      id: Response
      properties:
        result_code:
          type: string
        result_msg:
          type: string
        data:
          type: string
