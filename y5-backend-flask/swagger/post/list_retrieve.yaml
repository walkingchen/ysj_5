---
tags:
  - Post
parameters:
  - name: room_id
    in: body
    type: string
    required: true
    description: room id
  - name: timeline_type
    in: body
    type: string
    required: true
    description: timeline type

responses:
  200:
    description: return timeline
    schema:
      id: Response
      properties:
        result_code:
          type: string
        result_msg:
          type: string
        data:
          type: string

