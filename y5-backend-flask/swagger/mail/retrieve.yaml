---
tags:
  - Mail
parameters:
  - name: id
    in: path
    type: string
    required: true
    description: get mail with mail id
responses:
  200:
    description: return mail with details
    schema:
      id: Response
      properties:
        result_code:
          type: string
        result_msg:
          type: string
        data:
          type: string
